{
  "name": "simple-page-numbers",
  "version": "1.1.5",
  "description": "A simple JavaScript/TypeScript function that generates a spread of page numbers, based on the desired spread and the total number of pages.",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "test": "echo 'error no test specified' && exit 0"
  },
  "files": [
    "dist/**/*"
  ],
  "author": "Joshua Morris",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.5.1",
    "typescript": "^5.1.6"
  }
}
